# The in-kernel iosm driver claims the Intel XMM7360 but only exposes AT +
# proprietary RPC ports (no MBIM), and ModemManager cannot drive it. This
# project uses the userspace xmm7360-pci driver instead, so keep iosm out.
blacklist iosm
